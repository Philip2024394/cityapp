// /citydrivers — Main landing page for CityDrivers (brand-correct spelling).
// Mirrors /cityriders so both URLs serve the same customer marketing
// homepage. Re-export keeps a single source of truth — edit
// src/app/cityriders/page.tsx to change the layout.

export { default, metadata } from '../cityriders/page'
